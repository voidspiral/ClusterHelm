# Logical partition definitions (source of truth for nodesets)
#
# Format:  <logical_name> <nodeset>
# Resolve: slave/scripts/resolve-partition.py (reads partitions.conf from config/)
# Subsets (e.g. cn[1-3]) must expand inside an owned nodeset — validated at submit.
test cn[1-3]
