# Master agent runtime defaults
# Edit here

default_gateway cn1
default_partition test

# SSH timeouts (seconds)
submit_timeout 30
poll_timeout 15
ssh_connect_timeout 15

# Remote paths on slave gateways
remote_project /home/smt/agents
remote_run_slave scripts/run-slave.sh
