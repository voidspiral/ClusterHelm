# Slave gateway registry (source of truth for Master routing)
#
# Format:  <gateway_host> <logical_partition> <nodeset>
#   gateway_host      SSH host alias (see ~/.ssh/config)
#   logical_partition name in partitions.conf (must match)
#   nodeset           copy for display; authoritative mapping is partitions.conf
#
# Add one line per slave gateway. Master picks gateway by logical partition.
cn1 test cn[1-3]
